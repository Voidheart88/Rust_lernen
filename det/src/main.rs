/*
Ein Programm welches die Determinante einer Matrix ausgibt
*/

extern crate rand;

use rand::Rng;
use rand::prelude::*;               //Für den Zufallszahlengenerator
use std::time::Instant;             //Für Zeitmessungen

fn det(array: &[f32]){
    for i in array {
        
    };
}

fn main() {
    let array: [f32;4] = [1.0,2.0,3.0,4.0];
}
